<section class="hero">
  <div class="circle"></div>
  <div class="hero-card">
    <h1 class="hero-title"><span>Fallin</span> digital product agency</h1>
    <p class="hero-desc">
      Create live segments and target the right people for messages based on
      their behaviors.
    </p>
    <button class="hero-btn">discover</button>
  </div>
  <div class="hero-card">
    <img class="hero-img" src="/img/Fallin.png" alt="" />
  </div>
</section>
<style>
  .hero {
    display: flex;
    width: 100%;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    height: calc(100vh - 50px);
    text-align: center;
    position: relative;
  }
  .circle {
    position: absolute;
    width: 100vh;
    height: 100vh;
    border-radius: 50%;
    background-color: #01c686;
    z-index: -1;
    left: -45vh;
    top: -45vh;
    opacity: 0.1;
  }
  .hero-card {
    position: relative;
    display: flex;
    width: 100%;
    flex: 1 1;
    gap: 110px;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding: 50px;
  }
  .hero-title {
    text-transform: uppercase;
    font-size: 30px;
  }
  .hero-title span {
    color: green;
    font-style: italic;
  }
  .hero-desc {
    display: none;
  }
  .hero-btn {
    width: 100px;
    padding: 20px;
    background-color: green;
    color: white;
    font-weight: bold;
    text-transform: uppercase;
    border-radius: 10px;
    border: none;
    cursor: pointer;
    animation: button 1s ease infinite alternate;
  }
  @keyframes button {
    to {
      background-color: rgb(2, 63, 2);
    }
  }
  .hero-img {
    position: absolute;
    width: 100%;
    left: 0;
    right: 0;
    top: 10;
    bottom: 0;
    object-fit: cover;
    min-height: 100%;
    max-height: 100%;
    max-width: 100%;
    min-width: 100%;
  }

  @media screen and (min-width: 480px) {
    .hero {
      flex-direction: row;
      text-align: left;
    }
    .hero-title {
      font-size: 50px;
    }
    .hero-desc {
      display: block;
      font-size: 20px;
    }
    .hero-card {
      height: 100%;
      align-items: flex-start;
      padding: 100px;
    }
  }
</style>
